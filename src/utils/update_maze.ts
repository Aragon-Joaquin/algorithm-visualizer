import type { MazeNodes } from "../types"

// cMaze = current maze
// nMaze = new maze (maze thats going to be updated)
export function UpdateMaze(ctx: CanvasRenderingContext2D, cMaze: MazeNodes, nMaze: MazeNodes) {}
