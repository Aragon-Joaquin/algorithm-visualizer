/* 
    a disjoint set is practically a dataStructure that, compared to another, has no elements in common 
*/
export class DisjointSet<T> {
	public parent: T[] = []
	public size = new Map()

	find() {}

	union() {}
}
